apiVersion: v1
kind: Service
metadata:
  name: uhd-mcp-service
  namespace: mcp-services
  labels:
    app: uhd-mcp
spec:
  selector:
    app: uhd-mcp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
